# 任务清单：CORS 预检鉴权顺序

- [x] P1. 建立 OpenSpec 与允许/拒绝来源的 RED 预检回归。
- [x] P2. 仅放行真实 CORS 预检至内层中间件。
- [x] P3. 验证实际 SSE 无凭据请求仍为 401，未配置来源仍被 CORS 拒绝。
- [x] P4. 执行认证、身份、前端 SSE、编译和差异核查。
- [x] P5. 提交并快进本地候选，更新交接；不合并或推送 `main`。

## 版本记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-09-12 | 建立 CORS 预检鉴权顺序修复与 RED 用例。 |
| v1.1 | 2026-09-12 | 完成真实 CORS 预检的最小放行；定向认证与 CORS 回归 4 passed。 |
| v1.2 | 2026-09-12 | 认证、身份和兼容性后端回归 15 passed；前端 SSE 2 passed；生产构建、编译、UTF-8 替换字符与差异核查通过。 |
| v1.3 | 2026-09-12 | 已提交 `9a3e09c` 并快进本地候选引用；`main` 未合并、未推送。 |
