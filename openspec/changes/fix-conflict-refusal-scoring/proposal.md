# 变更提案：补齐冲突类安全拒答评测识别

## 背景

v7 核心离线评测中，冲突类样本已经给出“不能直接比较”“暂停确定性回答”“人工复核”等安全回答，但当前拒答评估器只识别少量词语，导致有效的安全拒答被误判为失败。

## 目标

- 让冲突类安全拒答的常见中文表达能够被确定性评估器识别。
- 保持现有答案级证据、数字、来源和工具轨迹评分逻辑不变。
- 通过固定测试证明不调用外部服务，且不以降低质量门禁阈值掩盖失败。

## 非目标

- 不改变评测集的发布状态或人工复核结论。
- 不修改正式环境、staging 运行时配置或 main 分支。
- 不把普通肯定回答误判为安全拒答。
