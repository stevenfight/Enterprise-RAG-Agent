# 任务清单：修复 HTTP 入口的研究任务提交

- [x] RTH-1. 确认公共 HTTP 入口、浏览器请求路径和后端日志，定位为 `crypto.randomUUID()` 在请求前失败。
- [x] RTH-2. 添加 HTTP 非安全上下文与服务端错误可见性的 RED 前端测试。
- [x] RTH-3. 以不依赖安全上下文的方式生成客户端任务 ID，并保留提交契约。
- [x] RTH-4. 展示真实提交失败原因，完成定向测试、lint 与构建。
- [x] RTH-5. 更新 TDD、交接文档和版本记录；部署候选前不切换正式入口。
