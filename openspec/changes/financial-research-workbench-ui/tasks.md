# 任务清单：财务研究工作台 UI 改造

## 工作包 A：基线与设计系统

- [x] A1. 已审核现有 `theme.ts`、`global.css`、`AppLayout`、`Sidebar`、`HeaderBar` 与五个路由页面，记录需保留的交互和硬编码色值。
- [x] A2. 已新建全红 TDD，覆盖主题 token、研究上下文、证据过程、响应式和可访问性。
- [ ] A3. 将色彩、间距、阴影、圆角和排版 token 收敛到主题与全局样式；将 `lightTheme`/`darkTheme` 改为接收主题模式与主体色的工厂配置，并迁移所有品牌相关马卡龙硬编码和 CSS 紫色回退值。语义色与多序列图表类别色保持独立。
- [x] A3.1. 在 `appStore`/主题 Hook 中增加受限的主体色枚举（金融青绿、深蓝）及首帧读取/浏览器持久化；与刷新后重置的 `researchContext` 分离。
- [x] A3.2. 将顶部现有深浅主题按钮升级为唯一“外观”面板，提供亮/暗模式和带文字标签的主体色选择；色板使用按钮、可见焦点和 `aria-pressed`，移除侧边栏重复开关，不改变主题切换能力。
- [x] A3.3. 在现有 `ConfigProvider` 树内原位更新动态主题 token，不给 `AppLayout`、`Outlet`、聊天容器、会话或消息列表添加随主题变化的 `key`；切换外观不得重建 SSE、丢失草稿、消息或滚动位置。
- [ ] A4. 调整全局导航、顶栏与页面容器；在现有 Zustand Store 中建立仅在应用运行期间有效、刷新即重置的 `researchContext`。`HeaderBar` 在 ≥1200px 内联展示只读摘要，在 768–1199px 收纳为上下文按钮，在 <768px 打开只读底部抽屉；首页 `ResearchContextBar` 作为唯一编辑入口，三种形态均不得遮挡其他控件。`topN`、`agentMaxSteps` 保持为首页高级选项，不接入浏览器或后端持久化。
- [x] A5. 复用 `AppLayout` 的 `checkHealth()`，将顶栏健康状态扩展为检查中、已就绪、暂不可用三态；初始未知状态不得误报为离线。

## 工作包 B：研究工作台

- [x] B1. 在不改变现有请求与流式逻辑的前提下，将 `ChatPage` 拆分为研究摘要条、主研究画布、证据面板和固定输入区。
- [x] B2. 复用现有公司选择、Agent 开关、消息、KPI、来源和输入组件，建立 `ResearchContextBar`、`ResearchWelcome`、`EvidencePanel` 等界面组件；来源分数统一标注为检索匹配度且说明为回答级证据，KPI 保留原始数值/单位，普通数值保持中性展示。
- [x] B3. 将 `ThoughtChainDrawer` 与 `MessageBubble` 的过程预览共同重构为“分析过程”，只展示经允许字段筛选、截断及敏感字段脱敏后的工具、输入摘要、观测、状态和风险提示，不渲染 `thought` 或原始过程数据。
- [x] B4. 新增 `AnalysisTraceStep` 安全过程视图模型，并在 `agentEvent` 中于 SSE 数据进入 `chatStore` 前完成投影；`Message.reasoningChain` 与过程抽屉改用该模型，过滤单 Agent `thought`、多 Agent `thought` 步骤内容和原始过程字段。工具名、Worker 名称和输入键必须经白名单映射为受控产品标签，未知值不得透传。
- [x] B5. 在 `chat-sessions` 加载时清理历史遗留过程字段、原始工具名和 Worker 名称并立即回写已清理会话，保留用户问题和最终回答。
- [x] B6. 保留流式回答、超时、错误、会话与滚动行为，并为工作台新增空、加载、错误和强制停止状态；研究范围仅标识下次提问，不向历史消息反向归因。

## 工作包 C：辅助页面一致性

- [x] C1. 升级图表页为分析成果页，使用现有 `ChartData` 增加类型/数量摘要、统一卡片和空状态引导。
- [x] C2. 升级资料库页的概览、上传和索引状态层级；只使用现有资料库 API 字段。
- [x] C3. 升级 DAG 页的高级分析流程定位、状态语义和窄屏降级。
- [x] C4. 升级系统状态页的分组、状态层级和错误/加载状态，保持只读能力。
- [ ] C5. 跨页面视觉收口：以研究型产品为统一母版，收敛标题区、卡片密度、筛选/操作区、空加载错误状态与主体色应用；在不重写既有功能的前提下完成 1440、1024、768、375 宽度视觉验收。
  - [x] C5.1. 为图表、资料库、DAG、系统状态页接入统一的产品域标签、标题层级、页面宽度和卡片节奏；聊天工作台保持独立结构。
  - [x] C5.2. 将图表筛选、资料上传、DAG 查询归入统一研究操作区，并统一图表/DAG 加载与空状态容器。
  - [ ] C5.3. 已调整为可选的改造前四断点基线记录；不再对即将被 C6 重组的旧首页投入独立微调。正式视觉验收统一由 C6.4 与 D3 完成。

- [ ] C6. 证据驱动研究 Copilot 总体重构：以默认浅色研究画布、可选深色专注模式和“问题—结论—财务证据—来源—追问”闭环重组现有界面；不新增后端契约，不将概念稿中的示意数据写入产品。
  - [ ] C6.1. 先为 `ChatPage` 的桌面主区/研究侧栏、唯一研究配置入口、受控会话面板、证据默认标签、互斥抽屉和无来源状态编写 RED 测试；不得形成全局导航、配置、会话、主区、证据五栏并列。公司选择直接订阅运行期 `researchContext.companyName`，不保留本地镜像或挂载回写；`agent-mode` 必须在 Store 创建时从浏览器偏好投影到 `researchContext.mode`，用户切换时同步更新，确保直接进入任意路由的顶栏、页面开关和请求参数一致。配置入口须有键盘语义；任一在途请求（RAG 或 Agent）期间禁用新建、切换、删除会话。捕获 `requestSessionId` 与 `requestId`，并为 Store 新增或改造显式目标会话的消息/过程/错误写入操作，回调不得按当时 `currentSessionId` 定位。实现时保持 SSE、输入与安全过程投影不变。
  - [ ] C6.1.5. 迁移 `ChatPage`、`MessageBubble`、`ChatInput`、`ThoughtChainDrawer` 中本轮触及的马卡龙品牌硬编码为现有主题语义变量，再进行布局重构；不得迁移语义色或多序列类别色。
  - [ ] C6.2. 将已有最终回答、文本识别 KPI、全局图表入口和回答级来源组织为结论区与金融证据区；主区只展示紧凑来源入口，完整来源只在侧栏显示。普通财务数值保持中性，缺失 KPI/来源不得补造内容，Agent 未返回来源时显示专属空状态，且不得伪造回答—图表关联。
  - [ ] C6.3. 从 `EvidencePanel` 提取可复用于桌面 `aside` 与窄屏 `Drawer` 的证据内容组件；复用 `ThoughtChainDrawer` 与安全 `AnalysisTraceStep` 建立具备 tab 语义的证据/分析过程按需标签。概念稿中的研究信号不在首期实现，除非另有结构化风险/假设数据契约。
  - [ ] C6.3.5. 在 `ChatPage` 维护当前会话的证据消息选择；历史回答可切换侧栏来源，切换/删除会话时重置。页面卸载时先使当前 `requestId` 失效，再取消当前 RAG 请求（`AbortSignal` 贯通 `queryQuestion`）、关闭当前 SSE、清理轮询/超时并结束加载态；RAG 与 SSE 的取消/过期回调均按请求标识抑制回写，取消错误不得生成系统错误消息。自动滚动遵守减少动态效果偏好。
  - [ ] C6.4. 在 1440、1024、768、375 宽度验证主侧栏/会话抽屉互斥、固定输入安全区域、长回答与表格滚动、外观切换和减少动画偏好。
    - [x] C6.4.1. 修复 375px 下顶栏操作区导致的横向最小宽度，保留研究范围、外观入口和健康状态的可访问名称。
    - [x] C6.4.2. 修复 375px 下研究摘要条操作文字并排造成的右侧裁切，窄屏仅隐藏冗余文字，保留所有操作的图标和无障碍名称。
  - [x] C6.5. 将空会话首页升级为真实研究范围驱动的研究启动页：展示当前公司范围和模式、示例问题及既有回答/证据/分析过程能力；不得展示虚构 KPI、市场结论、投资评级或示意公司数据。

## 工作包 D：质量验收与收口

- [ ] D1. 为新增或显著改造组件编写前端 TDD；先确认 RED，再实现并转 GREEN，并覆盖“选择公司/模式—流式回答—证据—分析过程—跨路由—刷新”的完整研究路径，以及旧会话过程字段清理与回写。
- [ ] D2. 迁移既有 `agentEvent`、`MessageBubble`、`ThoughtChainDrawer`、`HeaderBar` 和 `AppLayout` 测试：删除对原始 `thought`/过程内容与布尔健康状态的期望，替换为安全模型、持久化无敏感字段和健康三态断言；增加主体色按钮的键盘/语义状态、对比度及流式期间外观切换不丢失状态的断言；不得删除既有用例规避回归。
- [ ] D3. 在现有开发环境中于 1440、1024、768、375 宽度完成视觉截图验收，检查上下文、阅读、证据和固定输入区；移动端固定输入区同时检查安全区域与键盘遮挡；首期不为截图验收新增自动化框架或依赖。
- [ ] D4. 验证键盘焦点、对比度、`prefers-reduced-motion`、空状态、错误状态和长内容滚动。
- [x] D5. 执行前端全量 Vitest 与生产构建；记录既有警告但不扩大修复范围。
- [ ] D6. 更新 README、CHANGELOG、交接记录和本 TDD；仅在所有适用项通过后归档变更。

## 工作包 E：可核验证据增强（C7）

- [x] C7.1. 为回答级来源增加受限的命中文本摘要：后端仅从已用于回答的检索片段生成固定上限、无敏感字段的 excerpt；前端仅在已有证据卡片展开后展示，保留文件名、页码和检索匹配度语义。
- [x] C7.2. 为 C7.1 先编写后端契约与前端证据卡片 RED 测试，再实现并验证 RAG 返回路径；不得回传全文、原始工具数据或凭文件名补造章节信息。
- [x] C7.3. 回答级多公司对比工作区：仅在后端提供结构化指标、单位、公司、年份与逐项来源映射后启动，禁止从 LLM Markdown 或全局图表反向推断。
  - [x] C7.3.1. 完成数据前置核查：本地三份官方 2024 年报含三大运营商营业收入的原文与页码，可作为首批受限事实集；旧图表仅作历史成果，不作为事实来源。
  - [x] C7.3.2. 先为受限事实注册表、比较 API 和回答关联编写全 RED 契约测试；首批只支持 `营业收入` / `2024` / 中国移动、中国联通、中国电信，未登记指标必须明确不返回比较工作区。
  - [x] C7.3.3. 为回答气泡增加已核验比较卡片：仅消费回答随附的受限事实，显示统一单位、逐公司数值和文件页码；没有比较事实时不显示，禁止回退解析 LLM 文本或历史图表。
- [x] C7.4. 审查修正：当上下文 token 上限截断最后一个片段时，来源摘要只可使用实际写入上下文的截断文本，不能泄露该片段未进入模型的尾部内容。
- [x] C7.5. 审查修正：统一前后端 `scores` 契约，允许后端已有的非数值状态字段，同时仅将数值 `hybrid` 用于检索匹配度百分比。
- [x] C7.6. 审查修正：过滤未进入上下文的来源后，保留原始来源编号，确保模型回答中的 `[来源N]` 与返回证据可以一一对应。

## 决策门

- [x] G1. 主体色改为可切换外观偏好：金融青绿为默认，深蓝为可选；不提供任意自定义取色。
- [x] G2. 已确认首期仅改前端视觉与信息架构，不新增文档公司/年度/章节等后端字段。

## 版本记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-08-25 | 创建财务研究工作台 UI 改造方案与全红 TDD。 |
| v1.1 | 2026-08-25 | 方案自查：补充会话级研究上下文、检索匹配度标签、双入口过程脱敏和移动端键盘验收。 |
| v1.2 | 2026-08-25 | 二次核查：明确刷新边界、财务单位/颜色语义、回答级证据范围、过程摘要脱敏与端到端验收路径。 |
| v1.3 | 2026-08-25 | 三次核查：补齐全局上下文语义、健康状态三态、过程数据写入前安全投影及浏览器历史会话清理。 |
| v1.4 | 2026-08-25 | 四次核查：明确安全模型在 `agentEvent` 的落点、旧会话清理回写及既有测试迁移要求。 |
| v1.5 | 2026-08-25 | 五次核查：补齐窄屏全局上下文入口及工具/Worker 名称、输入键的白名单映射边界。 |
| v1.6 | 2026-08-25 | 主体色改造：金融青绿/深蓝可即时切换，明确 token 工厂、持久化、唯一外观入口与硬编码迁移边界。 |
| v1.7 | 2026-08-25 | 六次核查：补齐主体色选择的可访问性、对比度依据及流式回答期间外观切换不重挂载约束。 |
| v1.8 | 2026-08-25 | 实施 A3.1：新增受限主体色工厂、首帧外观偏好恢复与浏览器持久化，并完成对应红绿测试。 |
| v1.9 | 2026-08-25 | 实施 A3.2：顶栏改为唯一外观面板，保留亮暗切换并加入受控主体色按钮，移除侧边栏重复入口。 |
| v1.10 | 2026-08-25 | 实施 A3.3：布局使用动态主题工厂与页面主体色 CSS 变量，并保持布局与聊天树不因外观切换重挂载。 |
| v1.11 | 2026-08-25 | 实施审查修正：主题变量上移至布局根节点，补齐 Menu/Button/Slider 与侧栏、全局样式的主体色迁移；其余页面硬编码留待逐页改造。 |
| v1.12 | 2026-08-25 | 实施 A5：健康状态由布尔值升级为检查中、已就绪、暂不可用三态，避免初始状态误报离线。 |
| v1.13 | 2026-08-25 | 安全过程审查：确认消息组件与多 Agent SSE 仍有原始过程字段路径；补充多 Agent 受控摘要与禁止持久化原始运行对象的实施项。 |
| v1.14 | 2026-08-25 | 完成 B1–B3：研究摘要、欢迎区、回答级证据和安全分析过程已落地；窄屏隐藏编辑配置栏并以会话抽屉保留会话操作。 |
| v1.15 | 2026-08-25 | 完成 B6：SSE 连接失败时清理本次轮询与超时计时器，避免旧请求回调重复报错或干扰后续连接。 |
| v1.16 | 2026-08-25 | 完成 C1：图表页调整为分析成果页，按现有图表类型汇总成果数量并统一使用状态概览卡片。 |
| v1.17 | 2026-08-25 | 完成 C2：资料库明确上传资料与资料清单层级，保留真实文档数和现有索引概览，不扩展数据字段。 |
| v1.18 | 2026-08-25 | 完成 C3：DAG 页明确高级分析流程定位，窄屏隐藏复杂画布并保留摘要、批次与图例。 |
| v1.19 | 2026-08-25 | 完成 C4：系统状态按研究可用性、模型与检索工具、运行配置重组，加载态保持统一页面框架。 |
| v1.20 | 2026-08-25 | 计划调整：新增 C5 跨页面视觉收口，回应单页优化不足以形成整体高级感的问题。 |
| v1.21 | 2026-08-25 | 实施 C5.1–C5.2：四个辅助页采用统一研究型标题母版；资料上传、图表筛选和 DAG 查询使用统一操作区，图表/DAG 使用统一空加载状态容器。 |
| v1.22 | 2026-08-25 | 方案调整：确定“证据驱动研究 Copilot”为下一阶段整体母版；补充主工作台、辅助页职责、数据边界、渐进实施批次和 TDD 验收门。 |
| v1.23 | 2026-08-25 | 方案二次核查修正：识别现有首页多栏结构与缺失的研究信号数据契约；改为先收敛配置/会话入口、提升会话抽屉控制权，并将 Signals 延后。 |
| v1.24 | 2026-08-25 | 方案复核修正：明确 KPI 为文本识别结果、图表无回答级关联；补充桌面 `aside` 复用、聊天组件 token 收口及研究配置入口的键盘语义。 |
| v1.25 | 2026-08-25 | 实施顺序复核：将 C5.3 调整为可选的改造前基线，避免在旧首页上重复微调；四断点正式验收统一由 C6.4/D3 承担。 |
| v1.26 | 2026-08-25 | 运行路径复核：明确 Agent SSE 无来源边界、流式期间会话锁定和来源去重规则，并补充侧栏标签的无障碍语义。 |
| v1.27 | 2026-08-25 | 历史与生命周期复核：补充按回答选择证据、会话切换重置、离页取消 SSE/计时器及减少动态效果下的即时滚动。 |
| v1.28 | 2026-08-25 | 状态来源复核：明确公司范围以运行期 `researchContext` 为唯一来源并支持跨路由返回；区分公司范围、Agent/步数偏好、当前页高级选项与证据选择的持久化边界。 |
| v1.29 | 2026-08-25 | 状态模型复核：消除 Agent 模式在运行期上下文与浏览器偏好之间的双来源歧义；规定偏好加载后的运行期投影及切换同步，保证顶栏、开关和请求参数一致。 |
| v1.30 | 2026-08-25 | 异步归属复核：将会话锁定与离页取消从仅 Agent SSE 扩展到 RAG 请求；规定 AbortSignal 贯通及不可取消回调的请求标识抑制，避免错误会话写入。 |
| v1.31 | 2026-08-25 | Store 写入复核：确认现有消息写入按回调时的活动会话定位；补充请求会话/请求标识显式传递、RAG 与 SSE 双重过期拦截，以及取消错误静默处理。 |
| v1.32 | 2026-08-25 | 一致性复核：统一 1200–1439px 侧栏默认并列、按需收起的响应式规则，并同步离页验收覆盖 RAG 与 SSE。 |
| v1.33 | 2026-08-25 | 初始化路径复核：补齐直接进入非首页路由时的 Agent 模式 Store 初始化；移除公司范围页面本地镜像，避免挂载回写覆盖运行期上下文。 |
| v1.34 | 2026-08-25 | C6.1 局部实施：配置栏改为研究摘要条唯一可访问入口所打开的受控抽屉；会话、证据、过程抽屉统一互斥，流式期间冻结会话操作，并补齐请求取消回写抑制测试。 |
| v1.35 | 2026-08-25 | C6.1 回归验证：确认辅助路由模式摘要、证据空状态和页面受控会话抽屉符合既定边界；未扩大业务实现范围。 |
| v1.36 | 2026-08-25 | C6.2 局部实施：主回答区来源展示收敛为紧凑证据入口，完整来源卡片仅在证据面板显示；支持按历史回答选择证据并在会话切换时重置。 |
| v1.37 | 2026-08-25 | C6.3 局部实施：抽取可复用 `EvidenceContent`，桌面端以语义证据侧栏展示，窄屏继续复用证据抽屉；分析过程标签重构仍待实施。 |
| v1.38 | 2026-08-25 | C6.3 局部实施：新增安全 `AnalysisTraceContent`，桌面侧栏提供证据与分析过程标签并保留键盘语义；移动端仍使用现有互斥抽屉。 |
| v1.39 | 2026-08-25 | C6.4 局部实施：四断点截图发现并修复窄屏常驻侧栏导致的工作台裁切，欢迎区改为可收缩布局；最终人工截图复核待稳定浏览器会话完成。 |
| v1.40 | 2026-08-25 | 新增 C6.5：首页不再仅作为普通欢迎态，改为基于真实研究范围的启动页；明确不以虚构财务数据装饰界面。 |
| v1.41 | 2026-08-25 | C6.4 断点验收发现 375px 顶栏操作区造成工作台横向裁切；补充可收缩操作标签与视觉回归要求。 |
| v1.42 | 2026-08-25 | 继续 375px 验收：顶栏已收缩，但研究摘要条操作文字仍裁切，补充第二处窄屏压缩任务。 |
| v1.43 | 2026-08-25 | 追加 C7 证据增强路线：先交付受限命中摘要，再等待结构化指标—来源契约后实施回答级多公司对比工作区。 |
| v1.44 | 2026-08-25 | 完成 C7.1/C7.2：来源仅保留实际进入 RAG 上下文的片段，受限摘要统一脱敏并在展开证据卡片中展示；相关前后端测试及生产构建通过。 |
| v1.45 | 2026-08-25 | C6.4 窄屏复核：补齐 375px 顶栏长研究摘要的隐藏规则，保留研究范围抽屉入口；组件、样式规则测试与生产构建通过。Chrome 无头截图受本机 GPU 沙箱阻断，完整人工截图验收仍待可用浏览器会话。 |
| v1.46 | 2026-08-25 | 变更审查发现 C7 截断来源摘要边界与前后端评分类型契约缺口；新增 C7.4/C7.5，先以 RED 测试修正。 |
| v1.47 | 2026-08-25 | 完成 C7.4/C7.5：摘要严格限定为实际进入上下文的文本，前端评分契约支持后端状态字符串；相关前后端测试和生产构建通过。 |
| v1.48 | 2026-08-25 | 全量前端审查发现启动页提取时遗失既有快捷提问样式钩子；在不改变新布局或点击行为前提下恢复 `quick-command-chip` 兼容类。 |
| v1.49 | 2026-08-25 | 二轮审查发现 C7 过滤来源后会重新编号，可能破坏回答引用与证据卡片的对应关系；新增 C7.6 RED 验收。 |
| v1.50 | 2026-08-25 | 完成 C7.6：返回来源保留原始上下文编号，回答引用与证据卡片重新一一对应；相关前后端测试和生产构建通过。 |
| v1.51 | 2026-08-25 | C6.4 截图验收第三次环境尝试：独立 Edge 无头会话与此前两次 Chrome 一样受 GPU 沙箱异常阻断，未生成可用截图；保持 C6.4/D3 未完成，不将自动化测试替代视觉验收。 |
| v1.52 | 2026-08-25 | C7.3 前置核查完成：三份官方年报存在可核验的 2024 营业收入原文及页码，确定以受限事实注册表而非旧图表或 LLM Markdown 作为首批结构化比较数据源。 |
| v1.53 | 2026-08-25 | C7.3.2 第一阶段：新增只读受限事实注册表及 RED→GREEN 测试，首批仅登记三大运营商 2024 营业收入及逐项年报来源；API 与回答关联仍未接入。 |
| v1.54 | 2026-08-25 | C7.3.2 第二阶段：新增已核验比较 API，仅返回登记事实；回答关联和前端工作区仍待实现。 |
| v1.55 | 2026-08-25 | 为 C7.3.3 补充回答级已核验比较卡片的 RED 验收：卡片只显示随回答返回的受限事实及逐项出处，缺失比较事实时必须不渲染。 |
| v1.56 | 2026-08-25 | 完成 C7.3.2/C7.3.3：RAG 回答附带受限登记事实，前端以回答级核验卡片呈现数值与年报页码；后端 11 项、前端 184 项测试和生产构建通过。 |
| v1.57 | 2026-08-25 | C6.4/D3 第四次环境验证：Edge 使用 `--headless=new`、软件渲染与虚拟时间参数仍因 GPU 进程退出而未生成截图；保留视觉验收未完成，不以测试替代截图。 |
| v1.58 | 2026-08-25 | 代码审查修正：移除 RAG 请求 `finally` 中的不安全控制流，修复图表主题 option 的 Hook 依赖与数值正则冗余转义；相关测试、lint 和构建通过。 |
| v1.59 | 2026-08-25 | 浏览器控制台核查发现两处 Ant Design `Drawer width` 弃用警告；新增 CP-R24 RED 验收，改用 `size` 时必须保持原有像素宽度。 |
| v1.60 | 2026-08-25 | 完成 CP-R24：研究配置与会话抽屉已迁移至等宽 `size` API，浏览器弃用警告回归测试、lint 和生产构建通过；浏览器扩展异步监听错误不归属项目。 |
