# 任务

- [x] 1. 建立 SSE 跨源 HttpOnly 会话的 RED 契约测试。
- [x] 2. 恢复 EventSource `withCredentials` 实现并验证 URL 不含身份令牌。
- [x] 3. 恢复路由依赖声明与锁文件至 HEAD 的 7.18.3 基线。
- [x] 4. 运行定向测试、类型/构建和差异检查。
- [x] 5. 更新交接记录；恢复结果与 HEAD 完全一致，未形成可提交的源码或锁文件变更。
