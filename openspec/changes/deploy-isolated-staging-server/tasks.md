# 任务清单：部署隔离服务器测试栈

- [x] D1. 建立部署 OpenSpec 与全线 RED TDD 条目。
- [x] D2. 复核服务器目标目录、端口、资源和正式容器前置状态。
- [x] D3. 上传候选 `5e17993`、创建独立测试环境文件并启动 Compose。
- [x] D3.1 记录授权后的资源处置边界：仅可回收 BuildKit 缓存与悬空无标签镜像；不得清理容器、卷、网络、正式镜像或项目数据。
- [x] D3.2 审计并最小化清理可回收 Docker 资源，复核正式栈无影响。
- [x] D3.3 记录后端启动前强制要求 `DASHSCOPE_API_KEY` 的 RED 证据；测试健康/鉴权验收将仅使用不可用于 Provider 的占位值，绝不调用 Agent 业务请求。
- [x] D4. 执行健康、登录、SSE 未授权、CORS 预检及正式栈隔离验收。
- [x] D5. 更新 TDD、交接与候选分支部署记录；不合并或推送主分支。

## 版本记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-09-12 | 创建隔离服务器部署记录，等待运行态前置检查。 |
| v1.1 | 2026-09-12 | 已上传候选并创建独立目录；Compose 构建超过受限等待窗口未产出容器，已停止测试构建以保护同机正式服务。 |
| v1.2 | 2026-09-12 | 用户已授权处置可回收 Docker 构建缓存/无用镜像；清理范围不含容器、卷、网络、正式镜像和项目数据。 |
| v1.3 | 2026-09-12 | 构建完成但后端因缺少 Provider Key 在启动前退出；测试环境只补不可用占位值，验证不调用 Provider 的健康与鉴权路径。 |
| v1.4 | 2026-09-12 | 已释放受限缓存和悬空镜像，隔离栈运行态验收通过；测试与正式容器、端口和数据挂载保持隔离。 |
