# 变更提案：部署隔离服务器测试栈

## 背景

正式服务器的 `/opt/enterprise-rag` 正在运行旧版正式容器。候选提交 `5e17993` 需要在同一服务器完成运行态验收，但不得更新正式目录、正式分支、正式数据或正式容器。

## 本次变更

1. 将候选提交的源码归档部署到独立目录 `/opt/enterprise-rag-staging`。
2. 使用独立 `.env.staging`、独立数据卷和回环端口 `18000`、`18081` 启动测试栈。
3. 验证健康检查、研究登录、SSE 未授权保护与 CORS 预检。
4. 记录候选提交标识和正式服务未受影响的证据。

## 明确不做

- 不执行正式目录中的 `git pull`、`docker compose up`、停止或重建。
- 不合并、推送或修改 `main`。
- 不读取或输出正式环境凭据及测试会话令牌。
