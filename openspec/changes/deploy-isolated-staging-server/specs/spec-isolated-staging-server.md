# 需求：隔离服务器测试部署

## R1：候选来源可追溯

测试目录必须来自指定候选提交，并保存不含敏感信息的提交标识。

## R2：运行态与正式栈隔离

测试服务只能使用 `/opt/enterprise-rag-staging`、独立容器与网络，以及回环端口 `18000`、`18081`；不得修改正式目录或正式容器。

## R3：可执行的最小验收

测试栈必须通过后端健康检查、研究登录、未授权 SSE 拒绝和配置来源允许的 CORS 预检；验收后正式容器仍保持运行。
