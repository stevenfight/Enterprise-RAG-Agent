# 设计

## 分类规则

1. 仍被现有代码、测试、交接文档或其他 OpenSpec 引用的文件属于必须保留。
2. 描述现行安全、部署、来源审计或评测门禁但没有等价替代证据的文件属于必须保留。
3. 只有在存在明确归档/替代文件、没有反向引用且不承载当前验收证据时，才可归档后删除。
4. 无法由仓库事实判断的发布集记录标为需要人工决策，不以推测删除。

## 输出

生成一份机器可核对的 Markdown 清单，记录删除路径、引用证据、替代位置、分类和建议动作；随后只恢复必须保留的文件，保留其他候选删除等待下一切片。
