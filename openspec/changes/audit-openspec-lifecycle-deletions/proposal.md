# 审计 OpenSpec 批量删除与发布集生命周期

## 背景

候选工作区删除了大量历史 OpenSpec 变更目录和 v7 核心规格。它们包含部署、鉴权、跨域、来源审计和评测门禁的交付证据；当前没有一份已验证的生命周期清单证明这些证据都已被替代。

## 目标

建立可复验的删除清单，按引用关系、当前实现和替代文档将每个目录分类为“必须保留”“可归档后删除”或“需要人工决策”。只处理证据充分的项。

## 非目标

- 不改变产品代码、测试、部署或运行时配置。
- 不直接提交现有批量删除。
- 不删除历史 Git 提交或远程分支。
