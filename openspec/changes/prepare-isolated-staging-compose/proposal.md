# 变更提案：准备隔离服务器测试 Compose 配置

## 背景

正式服务器已运行旧版 `rag-backend` 与 `rag-frontend`。候选版本需要在不触碰正式容器、正式数据和正式端口的前提下完成受控测试部署准备。

## 目标

1. 提供独立于正式 Compose 的测试服务定义。
2. 使用本机回环端口、独立容器名、网络和数据目录。
3. 提供不含真实密钥的测试环境变量模板。

## 非目标

- 不上传代码、不连接服务器写入、不启动容器。
- 不复制正式数据、索引、配置或凭据。
- 不改变正式 Compose、Nginx、云防火墙或正式环境变量。
