# 设计

1. 先使用现有连接生成并设置服务器账号的新随机密码，再以新密码重新建立 SSH 连接确认轮换成功。
2. 新密码仅写入当前 Windows 用户的 `ENTERPRISE_RAG_SSH_PASSWORD` 环境变量，且不输出到终端、文档或仓库。
3. `deploy.py` 通过 `os.environ` 强制读取该变量；变量缺失或为空时立即抛出明确错误，不保留硬编码回退。
4. 仅查询隔离环境健康端点验证新凭据可用；不改写正式容器或数据。
