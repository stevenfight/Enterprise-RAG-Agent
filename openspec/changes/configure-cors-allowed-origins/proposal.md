# 变更提案：配置化 CORS 允许来源

## 背景

当前 CORS 仅硬编码两个本地 Vite 来源。预检鉴权顺序已修复，但受控的 HTTPS 前端部署域名仍无法通过配置加入白名单。

## 目标

1. 通过无敏感值环境变量配置允许的浏览器来源。
2. 未配置时保持现有两个本地开发来源。
3. 拒绝空值、通配符、路径及非 HTTP(S) 值，继续与凭据 Cookie 配合使用。

## 非目标

- 不自动猜测或放宽部署域名。
- 不使用 `*`，不修改 Cookie、SSE 协议或 API 鉴权。
- 不执行真实部署或浏览器登录。
