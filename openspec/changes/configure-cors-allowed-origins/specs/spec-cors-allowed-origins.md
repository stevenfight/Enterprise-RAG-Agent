# 需求：配置化 CORS 允许来源

## R1：保留安全的默认开发来源

系统在未配置 `CORS_ALLOWED_ORIGINS` 时必须仅允许现有的本地开发来源。

## R2：支持显式部署来源

系统必须接受逗号分隔的 HTTP(S) 来源，并将其交给既有 CORS 中间件。

## R3：非法配置失败关闭

空值、通配符、重复值、路径、查询参数、片段及非 HTTP(S) 来源必须在启动配置解析时被拒绝。
