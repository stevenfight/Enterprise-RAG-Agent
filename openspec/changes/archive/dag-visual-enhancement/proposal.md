# 变更提案：DAG 看板视觉增强

## 背景

DAG 看板当前已经能够展示任务节点、连线和节点详情，但页面顶部缺少任务概览，执行批次信息主要以文字标签呈现，节点状态层级不够直观。

## 目标

- 增加任务摘要卡片，帮助用户快速了解当前 DAG 任务。
- 使用状态颜色增强节点执行状态识别。
- 将执行批次以时间线形式呈现。
- 保留现有 DAG 规划请求、节点详情和图例逻辑。
- 不修改后端接口和任务数据结构。

## 非目标

- 不改变 DAG 布局算法。
- 不新增实时任务轮询。
- 不重构 G6 图形渲染实现。
