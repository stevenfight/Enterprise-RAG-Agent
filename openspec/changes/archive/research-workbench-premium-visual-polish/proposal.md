# 研究工作台高级视觉统一

## 背景

当前研究工作台的核心功能已经可用，但多个页面仍沿用偏紫的马卡龙视觉、过大的留白和不一致的输入区域，削弱了企业级财务研究产品应有的专业感与信息密度。

## 目标

- 统一浅色与暗色模式的页面层级、边界、字体层级和间距。
- 让聊天输入框、辅助页卡片和数据摘要共享同一套表面语义。
- 保留既有路由、交互、数据展示和主体色切换能力，不改变后端接口。
- 在桌面、平板和移动宽度下维持清晰的信息优先级。

## 非目标

- 不新增业务功能。
- 不重写图表、知识库、DAG 或设置页的数据逻辑。
- 不替换现有 Ant Design 组件库。
