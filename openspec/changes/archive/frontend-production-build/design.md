# 设计方案：恢复前端生产构建

## 处理流程

1. 运行前端生产构建和 TypeScript 检查，记录完整错误。
2. 按文件逐项修复类型定义或测试辅助数据。
3. 每项修复保持运行时逻辑不变，优先使用现有类型和组件 API。
4. 运行相关测试，再运行全量测试、类型检查和生产构建。

## 约束

- 不使用 `any` 绕过类型检查。
- 不删除测试来规避构建错误。
- 不修改构建脚本以隐藏错误。
- 对第三方类型兼容问题采用项目现有依赖版本支持的写法。
