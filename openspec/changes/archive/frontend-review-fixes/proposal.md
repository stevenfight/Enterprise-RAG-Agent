# 变更提案：前端代码审查问题修复

## 背景

第一阶段重复逻辑收敛完成后，代码审查发现三项低风险问题：服务层反向依赖组件类型、DAG 节点类型约束被放宽、图表表格分页测试依赖 Ant Design 内部无障碍结构。

## 目标

- 将 `ChartData` 下沉到独立类型模块，解除服务层对组件层的依赖。
- 统一复用 DAG 节点类型联合类型，恢复前端类型约束。
- 将分页测试改为验证用户可见的分页内容，降低对组件内部结构的依赖。

## 非目标

- 不改变后端接口、响应结构、页面路由和业务行为。
- 不新增第三方依赖。
- 不处理其他后端或前端重构事项。
