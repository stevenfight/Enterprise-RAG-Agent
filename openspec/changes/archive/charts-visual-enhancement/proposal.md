# 变更提案：图表中心视觉增强

## 背景

图表中心当前以单列方式展示图表，卡片缺少数据周期和来源等上下文信息，空状态也缺少引导。

## 目标

- 将图表展示调整为响应式网格，提升桌面端空间利用率。
- 在图表卡片中补充数据周期、来源和生成时间等元信息（使用已有字段）。
- 优化无图表时的空状态文案和操作引导。
- 保留图表/表格切换、类型筛选和现有数据渲染逻辑。

## 非目标

- 不新增后端接口和数据字段。
- 不新增图表下载或复制功能。
- 不改变图表数据转换逻辑。
