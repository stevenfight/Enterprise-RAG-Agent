# 保留评测来源审计兼容接口

## 背景

V7 评测候选新增了只读基线就绪检查，但同时移除了既有来源文件、冻结清单和数据集绑定审计入口。旧入口已经用于验证评测来源的文件存在性、哈希、页数和绑定关系，不能因为新增基线检查而静默失效。

## 目标

在不改变新基线就绪检查语义的前提下，恢复旧 CLI 参数、审计模块和公共导出，使常规离线评测继续将来源审计结果写入报告并影响退出码。

## 非目标

- 不变更评测样本、来源清单或发布状态。
- 不调用 Provider、网络或修改来源文件。
- 不处理部署、前端或多模态迁移。
