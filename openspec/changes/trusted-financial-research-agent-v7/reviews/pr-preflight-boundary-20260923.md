# 本地候选 PR 前边界清单（2026-09-23）

## 本地拓扑快照

- 当前分支：`codex/next-plan-20260901`。
- 当前本地 `origin/main` 引用与 HEAD 的共同基点：`7010598`。
- 基于未刷新远端引用，`origin/main...HEAD` 的左右提交计数为 `0 13`：候选没有落后本地记录的 `origin/main`，领先 13 个提交。
- 本地 `main` 停在 `d0498b1`，比本地 `origin/main` 少 71 个提交，不能作为 PR 基线判断依据。

## 可纳入候选的 13 个本地提交

1. `af73ad7`：对齐候选与本地记录的 `origin/main`。
2. `f4509d0`、`a46ecd3`、`5d66a3b`：研究报告可发现性和前端工作台相关改进。
3. `0c3adfe`、`46e0bb5`：评测就绪度和来源审计兼容规格。
4. `47109f6`：视觉制品与发布集。
5. `b81a447`、`3cc2fd4`、`b8c0725`：OpenSpec/历史文档审计记录。
6. `eb9ca07`、`aa8d7a0`、`35cbd57`：评测导入、Agent 工具测试边界和 Streamlit 展示修复。

## 明确排除

- 工作区中未提交的 `CHANGELOG.md` 和 7 份 V7 主文档：来源或状态冲突尚未逐项处置。
- `.pytest_tmp/`、`.worktrees/`、`tmp/`：运行或工作副产物。
- 三份报告导出 proposal 和历史 A-GATE 审计附件：未形成完整、可验证的产品变更。

## PR 前必做复验

1. 先执行只读 `git fetch origin`，以最新 `origin/main` 重算共同基点、领先/落后提交和冲突状态。
2. 在当前候选 SHA 上复验后端定向集合、前端测试/生产构建、OpenSpec 严格校验和依赖安全审计；历史单项结果不得替代这次候选汇总复验。
3. 确认工作区排除项未被暂存，并以已验证 SHA 推送现有分支后再创建或更新面向 `main` 的 PR。
4. PR 创建后等待 `frontend`、`backend`、`workflow-lint` 三项远端检查；通过后仍需按发布范围决定外部验收，不能将 CI 等同于服务器或 Provider 验收。

## 本轮边界

本记录不授权 `git fetch`、推送、PR 创建、合并、部署或服务器写入；这些动作必须取得单独授权。
