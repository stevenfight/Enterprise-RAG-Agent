# 设计

1. 使用 HEAD 中已验证的严格 CORS 解析：仅允许唯一、无路径、无凭据的 HTTP(S) Origin；非法值失败关闭。
2. SSE 不再属于 API 匿名白名单；有效 Bearer 或服务端研究会话仍可放行。真实 CORS 预检请求单独交给 CORS 中间件。
3. `.env.staging` 恢复为 Git 忽略项；模板继续作为唯一可提交配置示例。
4. 先保留失败的保护测试作为 RED 证据，恢复最小实现后运行后端和前端路由回归。
