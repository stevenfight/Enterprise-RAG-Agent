# 集成刷新后的 origin/main 研究报告摘要改动

## 背景

远端 `origin/main` 已前进到 `0906001`，候选分支与其在研究工作台文件上产生三处合并冲突。两侧均包含研究报告可发现性能力，候选还包含后续的工作台布局与可访问性改进。

## 目标

将 `origin/main=0906001` 集成到现有候选分支，保留远端报告摘要行为和候选工作台改进，并以定向前端/研究回归验证。

## 非目标

- 不推送、不创建 PR、不合并 `main`、不部署。
- 不处理工作区中已排除的 V7 主文档和 CHANGELOG 草稿。
