# 保留 v5.19 兼容证据与隔离部署配置

## 背景

工作区删除了 v5.19 兼容基线清单、隔离 staging Compose 和 staging 环境模板，但当前没有等价替代物。删除会使历史兼容证据、隔离部署复现和测试入口不可用。

## 目标

恢复这三类受版本控制的非敏感配置及其 v5.19 合约测试，保留现有正式部署与用户凭据边界。

## 非目标

- 不运行部署、不修改服务器、Nginx、端口或 `.env.staging`。
- 不伪造缺失的 v5.19 运行时数据或认证成功响应。
- 不删除或归档其他历史 OpenSpec 目录。
