# 隔离评测模块的根包导入副作用

## 背景

评测 schema、数据集和 CLI 只需要轻量的评测模块；根包旧式 eager import 会连带初始化 RAG/Agent 依赖和监控配置，增加离线评测的副作用与环境要求。

## 目标

保留既有 `from src import ...` 公开导出兼容，同时让评测模块按需导入 RAG/Agent 导出，避免不需要模型或监控的离线路径被提前初始化。

## 非目标

- 不改变 RAG、Agent、监控或 Provider 的业务逻辑。
- 不修改依赖版本、真实凭据、部署配置或远程仓库。
