# 修复 Windows 深路径下页图临时文件渲染失败

## 背景

页图渲染器会在最终文件名后拼接 UUID 和 `.writing` 生成临时 PNG。深层 Windows 临时目录中，该临时路径可能超过 PyMuPDF 可写入长度，导致视觉制品仓库测试和实际渲染失败。

## 目标

保持写入后原子替换与失败清理语义，将临时 PNG 名称缩短到不依赖最终制品长哈希文件名。

## 非目标

- 不改变最终 PNG 或缩略图路径、制品 ID、缓存语义和数据库记录。
- 不修改视觉制品仓库、PDF 内容、Provider、部署或前端。
