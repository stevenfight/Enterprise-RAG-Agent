# 验收视觉证据链与发布集代际迁移

## 背景

候选工作区包含视觉制品、多模态页面理解、声明级视觉事实，以及文档/索引 generation 发布、提升、退休和删除协调器。该组尚未形成独立的可提交验证边界。

## 目标

按既有 M0/M1、视觉安全和发布集生命周期契约运行验收，识别阻塞项；仅对确定的缺失或回归做最小修复，形成可审查的功能切片。

## 非目标

- 不调用真实视觉 Provider，不上传真实文档，不部署。
- 不改前端整体样式、SSE、路由或旧来源审计接口。
- 不把本地 `data/v7`、临时目录或缓存提交为发布资产。
