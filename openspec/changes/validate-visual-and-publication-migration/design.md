# 设计

## 验收层次

1. M0/M1：存储架构、孤儿数据、页面制品访问与纵向链路。
2. 视觉：制品仓库、视觉事实、Provider 安全边界、图表/表格/扫描页回归。
3. 发布集：generation 绑定、提升、退休、删除协调和发布解析。
4. 集成：现有后端 API/检索测试与前端证据组件定向回归。

## 收口规则

- 测试通过且不依赖临时运行时数据的源代码/测试可形成候选提交。
- 所有失败必须区分为代码回归、测试夹具缺失、环境/Provider 依赖或超出本切片的旧问题。
- 没有完整验证之前，不删除旧路径或把 `data/v7` 作为提交内容。
