# TDD：普通 RAG 财务金额单位换算

| 编号 | 状态 | 验证内容 | 预期结果 |
|---|---|---|---|
| U-R01 | GREEN | 通用、对比、财务数据提示词的单位规则 | 三类提示词均要求先读取来源单位，包含百万元除以 100 的换算和 `1,040,759百万元 = 10,407.59亿元` 示例，不再声明原始单位一律为千元。 |
| U-R02 | GREEN | 运营商营收表格图表单位 | 本次查询生成的三运营商 2024 年营业收入表格中，中国移动显示为 `10,408` 亿元，而不是 `1,040.8` 亿元。 |

## 执行记录

| 日期 | 用例 | 结果 | 证据 |
|---|---|---|---|
| 2026-08-25 | U-R01 | RED | 新增 `tests/test_rag_prompt_unit_conversion.py` 后，现有普通 RAG 提示词缺少百万元规则且仍写死“原始单位为千元”。 |
| 2026-08-25 | U-R01 | GREEN | `python -m unittest tests.test_rag_prompt_unit_conversion -v` 通过；既有 `TestUnitConversion` 4 项通过。实际 `RAGGenerator.query` 三运营商对比返回中国移动 `10,407.59亿元`。 |
| 2026-08-25 | U-R02 | RED → GREEN | `tests/test_chart_unit_conversion.py` 先复现表格 JSON 中 `1,040.8` 的单位错误；修正为 `10,408` 后，图表、提示词和既有单位换算共 6 项测试通过。图表列表接口需要 API Key，未以未授权请求替代文件级契约验证。 |
